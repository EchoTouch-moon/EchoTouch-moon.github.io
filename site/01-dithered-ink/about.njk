---
layout: layouts/01-shell.njk
permalink: about.html
title: About
description: "About EchoTouch-moon: writing, code, and the machinery behind this site."
current: about
---
<h1 class="page-title">关于我</h1>

<div class="portrait-row">
  <img class="avatar" src="{{ sites.ink.github.avatar }}" width="108" height="108" alt="{{ sites.ink.handle }} 的头像">
  <div>
    <p><strong>你好，我是 {{ sites.ink.name }}（iris）。</strong></p>
    <p>GitHub 上活跃于 <a href="{{ sites.ink.github.url }}" target="_blank" rel="noopener">{{ sites.ink.handle }}</a>，主要做 AI 应用、TypeScript / Python 工具开发，以及 Obsidian 插件与算法学习相关的 side project。</p>
  </div>
</div>

<h2>联系方式</h2>
<ul>
  <li><strong>邮箱：</strong><a href="mailto:{{ sites.ink.email }}">{{ sites.ink.email }}</a></li>
  <li><strong>GitHub：</strong><a href="{{ sites.ink.github.url }}" target="_blank" rel="noopener">{{ sites.ink.github.user }}</a></li>
</ul>

<h2>GitHub 项目</h2>
<ul>
  {% for project in sites.ink.projects %}
    <li><strong><a href="{{ project.url }}" target="_blank" rel="noopener">{{ project.name }}</a>：</strong>{{ project.description }}</li>
  {% endfor %}
</ul>

<h2>站点说明 (Colophon)</h2>
<ul>
  <li><strong>字体排印：</strong>标题采用 Fraunces，正文采用 EB Garamond，代码与日期采用 IBM Plex Mono。</li>
  <li><strong>墨水画布：</strong>原创 WebGL 1-bit 点阵抖动引擎，支持《千里江山图》长卷、波浪、微雨、垂柳、流云等东方水墨意境。</li>
  <li><strong>构建架构：</strong>使用 Eleventy 将 Markdown 源文件直接编译为高性能纯静态页面，无重型前端框架、零多余追踪。</li>
  <li><strong>部署运维：</strong>托管于 <a href="{{ sites.ink.github.url }}" target="_blank" rel="noopener">GitHub Pages</a>，通过 GitHub Actions 工作流在代码推送后自动构建发布。</li>
</ul>
